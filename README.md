# AI Vision App (YOLO + Face Detection)

## Setup Instructions

1. Install dependencies:
   pip install -r requirements.txt

2. Run the app:
   streamlit run App.py

## Notes
- Make sure yolov8n.pt is in the same folder.
- Webcam access is required.
